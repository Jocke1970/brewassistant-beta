type: conditional
conditions:
  - entity: switch.brewzilla
    state: "on"
card:
  type: vertical-stack
  cards:
    - type: custom:button-card
      entity: button.brewassistant_mash_in_started
      name: BrewZilla Mash-In Controls
      icon: mdi:barley
      show_state: false
      show_label: true
      triggers_update:
        - button.brewassistant_mash_in_override
        - button.brewassistant_mash_in_started
        - button.brewassistant_mash_in_complete
        - binary_sensor.brewassistant_brewzilla_mash_in_gate_pending
        - sensor.brewassistant_brewzilla_control_reason
        - sensor.brewassistant_brewday_runtime_source
        - binary_sensor.brewzilla_profile_active
      tap_action:
        action: more-info
      label: |
        [[[
          function clean(v) {
            if (v === undefined || v === null) return null;
            const s = String(v).trim();
            return ['unknown','unavailable','none',''].includes(s.toLowerCase()) ? null : s;
          }
          function numAttr(attrs, key) {
            const n = Number(String(attrs?.[key] ?? '').replace(',', '.'));
            return Number.isFinite(n) ? n : null;
          }
          function fmt(v, unit = '°C', d = 1) {
            return Number.isFinite(v) ? `${v.toFixed(d)} ${unit}` : '—';
          }
          function boolish(v) {
            const s = String(v ?? '').toLowerCase();
            return v === true || ['on','true','yes','1'].includes(s);
          }
          const start = states['button.brewassistant_mash_in_started'];
          const complete = states['button.brewassistant_mash_in_complete'];
          const override = states['button.brewassistant_mash_in_override'];
          const oa = override?.attributes || {};
          const a = start?.attributes || complete?.attributes || {};
          const gate = states['binary_sensor.brewassistant_brewzilla_mash_in_gate_pending'];
          const oz = states['sensor.brewassistant_brewzilla_control_reason']?.attributes || {};
          const source = clean(states['sensor.brewassistant_brewday_runtime_source']?.state) || 'None';
          const rapt = source === 'RAPT BrewZilla Profile';
          const bf = source === 'Brewfather Brew Tracker';
          const raptAttrs = states['binary_sensor.brewzilla_profile_active']?.attributes || {};
          const gateState = clean(a.state || a.mash_in_gate_state || gate?.attributes?.state || gate?.attributes?.mash_in_gate_state || oz.mash_in_gate_state) || 'idle';
          const startedVisible = a.mash_in_started_visible === true;
          const completeVisible = a.mash_in_complete_visible === true || gateState === 'mash_in_started';
          const cleanHeat = boolish(oz.clean_heat_strike_active);
          const strike = numAttr(oz, 'heat_strike_target') ?? numAttr(oz, 'heat_strike_clean_control_target');
          const gateTemp = numAttr(oz, 'clean_heat_strike_gate_temperature');
          const gateDelta = numAttr(oz, 'clean_heat_strike_gate_delta_to_target');
          const safetyTemp = numAttr(oz, 'clean_heat_strike_safety_temperature');
          const safetyDelta = numAttr(oz, 'clean_heat_strike_safety_delta_to_target');
          const heat = numAttr(oz, 'desired_heat_utilization');
          const pump = numAttr(oz, 'desired_pump_utilization');
          const pumpReason = clean(oz.clean_heat_strike_pump_reason) || '—';
          const effective = clean(a.effective_target || a.mash_in_effective_target || gate?.attributes?.effective_target || gate?.attributes?.mash_in_effective_target) || '—';
          const next = clean(a.next_target || a.mash_in_next_target || gate?.attributes?.next_target || gate?.attributes?.mash_in_next_target) || clean(raptAttrs.next_step_target_temperature) || '—';
          const process = Number(oa.mash_in_override_process_temperature);
          const processAge = Number(oa.mash_in_override_process_temperature_age_seconds);
          const processFresh = oa.mash_in_override_process_temperature_fresh === true;
          const overrideAvailable = oa.mash_in_override_available === true;
          const ageText = Number.isFinite(processAge) ? `${Math.floor(processAge / 60)}m ${Math.round(processAge % 60)}s` : 'unknown';
          const sourceLabel = rapt ? 'RAPT Profile' : bf ? 'BrewTracker' : source === 'Manual Brewday' ? 'Manual' : 'no process source';
          const startOk = !!start;
          const completeOk = !!complete;
          let msg = `Waiting for mash-in readiness · source ${sourceLabel}.`;
          if (!startOk || !completeOk || !override) {
            msg = 'New button entities are missing — pull and perform a full HA restart.';
          } else if (oa.mash_in_override_in_scope === true && !processFresh) {
            msg = `RAPT Cloud MASH data is stale (${ageText}). BrewZilla is regulating locally. ${overrideAvailable ? 'Manual strike acceptance is available after physical verification.' : 'Waiting until WORT/local target is inside the safe override band.'}`;
          } else if (cleanHeat && gateState !== 'mash_in_started') {
            msg = 'Heatstrike is active. Fresh MASH/process data gives automatic READY inside ±1.0 °C. The manual fallback appears only inside ±2.0 °C.';
          } else if (startedVisible) {
            msg = rapt
              ? 'Strike is ready. Press MASH-IN STARTED when grain addition begins. RAPT remains on its manual Mash In step.'
              : bf
                ? 'Strike is ready. Press MASH-IN STARTED when grain addition begins. Brewfather CONTINUE is then the primary completion acknowledgement.'
                : 'Strike is ready. Press MASH-IN STARTED when grain addition begins.';
          } else if (completeVisible) {
            msg = rapt
              ? 'Mash-in started. When grain is mixed in, press MASH-IN COMPLETE in BA. Then advance the manual RAPT step on BrewZilla to Mash Rest.'
              : bf
                ? 'Mash-in started. When done, press CONTINUE in Brewfather. BA completes mash-in and starts circulation automatically.'
                : 'Mash-in started. When grain is mixed in, press MASH-IN COMPLETE in BA.';
          } else if (gateState === 'mash_in_complete') {
            msg = rapt
              ? 'BA mash-in is complete and circulation is released. Now advance RAPT manual Mash In on BrewZilla to Mash Rest.'
              : bf
                ? 'Mash-in is complete. Brewfather is running and BA has released normal mash circulation.'
                : 'Mash-in is complete and BA has released normal mash circulation.';
          }
          const staleLine = oa.mash_in_override_in_scope === true && !processFresh
            ? `<br>MASH ${fmt(process)} · stale ${ageText} · WORT ${fmt(Number(oa.mash_in_override_wort_temperature))} · local target ${fmt(Number(oa.mash_in_override_local_target))}`
            : '';
          const heatLine = cleanHeat
            ? `<br>strike ${fmt(strike)} · gate ${fmt(gateTemp)} / Δ ${fmt(gateDelta)} · safety ${fmt(safetyTemp)} / Δ ${fmt(safetyDelta)} · heat ${Number.isFinite(heat) ? heat.toFixed(0) + ' %' : '—'} · pump ${Number.isFinite(pump) ? pump.toFixed(0) + ' %' : '—'} (${pumpReason})`
            : '';
          return `${msg}<br>source ${sourceLabel} · state ${gateState} · effective ${effective}°C · next ${next}°C${staleLine}${heatLine}`;
        ]]]
      styles:
        card:
          - border-radius: 18px
          - padding: 12px
          - background: rgba(127,127,127,.075)
          - border: 1px solid rgba(127,127,127,.18)
        grid:
          - grid-template-areas: '"i n" "i l"'
          - grid-template-columns: 36px minmax(0,1fr)
          - column-gap: 10px
        icon:
          - width: 24px
          - opacity: 0.82
        name:
          - justify-self: start
          - font-size: 13px
          - font-weight: 950
        label:
          - justify-self: start
          - text-align: left
          - white-space: normal
          - font-size: 11px
          - font-weight: 760
          - line-height: 1.25
          - opacity: 0.78

    - type: custom:button-card
      entity: button.brewassistant_mash_in_override
      name: MANUALLY ACCEPT STRIKE
      icon: mdi:hand-okay
      show_state: false
      show_label: true
      label: Acknowledges only the READY gate · max ±2.0 °C
      tap_action:
        action: call-service
        service: button.press
        target:
          entity_id: button.brewassistant_mash_in_override
        confirmation:
          text: >-
            Verify strike temperature physically on BrewZilla/thermometer.
            This button only acknowledges Mash-In READY and does not change
            heat, pump or target. Continue with MASH-IN STARTED afterward.
      styles:
        card:
          - display: |
              [[[
                return entity?.attributes?.mash_in_override_available === true ? 'block' : 'none';
              ]]]
          - border-radius: 18px
          - padding: 12px
          - background: rgba(255,152,0,.16)
          - border: 1px solid rgba(255,152,0,.42)
        name:
          - font-size: 12px
          - font-weight: 950
        label:
          - white-space: normal
          - font-size: 10.5px
          - font-weight: 750
          - opacity: 0.76

    - type: horizontal-stack
      cards:
        - type: custom:button-card
          entity: button.brewassistant_mash_in_started
          name: MASH-IN STARTED
          icon: mdi:barley
          show_state: false
          show_label: true
          triggers_update:
            - button.brewassistant_mash_in_started
            - binary_sensor.brewassistant_brewzilla_mash_in_gate_pending
            - sensor.brewassistant_brewzilla_control_reason
            - sensor.brewassistant_brewday_runtime_source
          label: |
            [[[
              const a = states['button.brewassistant_mash_in_started']?.attributes || {};
              const oz = states['sensor.brewassistant_brewzilla_control_reason']?.attributes || {};
              const target = oz.heat_strike_target ?? oz.heat_strike_clean_control_target ?? a.effective_target ?? a.mash_in_effective_target ?? '—';
              const gate = oz.clean_heat_strike_gate_temperature ?? '—';
              const safety = oz.clean_heat_strike_safety_temperature ?? '—';
              return `Start physical mash-in · strike ${target}°C · gate ${gate}°C · safety ${safety}°C`;
            ]]]
          tap_action:
            action: call-service
            service: button.press
            target:
              entity_id: button.brewassistant_mash_in_started
            confirmation:
              text: >-
                Start mash-in now? Verify that the Mash/BLE/control probe is at
                strike target and that wort/kettle is not running away. BA releases
                strike target, keeps the pump off and targets the next mash step with
                limited anti-drop heat.
          styles:
            card:
              - display: |
                  [[[
                    const a = states['button.brewassistant_mash_in_started']?.attributes || {};
                    const gate = states['binary_sensor.brewassistant_brewzilla_mash_in_gate_pending'];
                    const state = a.state || a.mash_in_gate_state || gate?.attributes?.state || gate?.attributes?.mash_in_gate_state;
                    const visible = a.mash_in_started_visible === true || state === 'ready_for_mash_in' || gate?.state === 'on';
                    return visible ? 'block' : 'none';
                  ]]]
              - border-radius: 18px
              - padding: 12px
              - background: rgba(255,193,7,.12)
              - border: 1px solid rgba(255,193,7,.28)
            name:
              - font-size: 12px
              - font-weight: 950
            label:
              - white-space: normal
              - font-size: 10.5px
              - font-weight: 750
              - opacity: 0.74
        - type: custom:button-card
          entity: button.brewassistant_mash_in_complete
          name: MASH-IN COMPLETE
          icon: mdi:pump
          show_state: false
          show_label: true
          label: |
            [[[
              const a = states['button.brewassistant_mash_in_complete']?.attributes || {};
              const source = String(states['sensor.brewassistant_brewday_runtime_source']?.state || '');
              const target = a.effective_target ?? a.mash_in_effective_target ?? '—';
              if (source === 'RAPT BrewZilla Profile') return `BA releases mash circulation · target ${target}°C · then advance RAPT on BrewZilla`;
              if (source === 'Brewfather Brew Tracker') return `Fallback · primary is CONTINUE in Brewfather · target ${target}°C · pump 50%`;
              return `Complete mash-in · target ${target}°C · pump 50%`;
            ]]]
          tap_action:
            action: call-service
            service: button.press
            target:
              entity_id: button.brewassistant_mash_in_complete
            confirmation:
              text: >-
                Is physical mash-in complete? BA completes the Mash-In gate and
                releases normal mash circulation. If RAPT is the process source,
                advance the profile's manual Mash In step on BrewZilla afterward.
          styles:
            card:
              - display: |
                  [[[
                    const a = states['button.brewassistant_mash_in_complete']?.attributes || {};
                    const gate = states['binary_sensor.brewassistant_brewzilla_mash_in_gate_pending'];
                    const state = a.state || a.mash_in_gate_state || gate?.attributes?.state || gate?.attributes?.mash_in_gate_state;
                    const visible = a.mash_in_complete_visible === true || state === 'mash_in_started';
                    return visible ? 'block' : 'none';
                  ]]]
              - border-radius: 18px
              - padding: 12px
              - background: rgba(76,175,80,.12)
              - border: 1px solid rgba(76,175,80,.26)
            name:
              - font-size: 12px
              - font-weight: 950
            label:
              - white-space: normal
              - font-size: 10.5px
              - font-weight: 750
              - opacity: 0.74
